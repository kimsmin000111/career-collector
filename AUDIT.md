# 수집 파이프라인 점검 기록

## 실제 데이터 흐름

`config/companies.yaml`의 출처 목록 → 출처별 `collectors/` 어댑터 → `core/filters.py` 규칙 분류 → `core/storage.py` SQLite upsert·이력 보존 → `core/export.py` 공개 JSON → GitHub `collector-data` 브랜치 → 사이트 `lib/collected-feed.ts` 검증·D1 캐시 → `components/desk.tsx` 필터·표시 순서로 동작합니다.

개인 지원 상태·메모·숨김 정보는 사이트 D1의 사용자별 테이블에 있으며 공개 수집 DB와 분리되어 있습니다. 수집 데이터 갱신과 사이트 배포는 이 테이블을 삭제하거나 덮어쓰지 않습니다.

## Root Cause Matrix

| 우선순위 | 증상 | 원인 | 근거 | 수정 |
|---|---|---|---|---|
| P0 | 관리 목록은 많지만 실제 자동 수집은 3개뿐 | 비활성 후보를 실제 감시 대상으로 오해할 수 있는 표시 | 기존 health의 attempted=3, registry 76 | 활성·확장 검토·전체 등록 수를 분리하고 `coverage.json` 추가 |
| P1 | 한국교통안전공단 해운대검사소 공고 누락 | JOB-ALIO 화면 목록 앞부분만 조회하고 상세 페이지를 건별 재조회 | 재정경제부 ALIO 공개 JSON의 기계 NCS 결과에서 공고번호 304569 확인 | 관련 NCS 8개 공식 JSON 조회, 공고번호 중복 제거, 목록의 자격·지역·날짜 직접 저장 |
| P1 | 한화 계열 신입 직무 누락 | 한화인 공식 공개 데이터 어댑터 부재 | 한화인 목록 API에서 신입·인턴 공고 확인 | 공고를 세부 직무 단위로 분리하는 한화 어댑터 추가 |
| P1 | 삼성 다음 공채를 자동 감지할 수 없음 | Samsung Careers 후보만 있고 수집 미활성 | 공식 목록·상세 공개 데이터 구조 확인 | 신입·인턴 전용 삼성 어댑터 추가. 0건도 구조 정상 여부를 검증 |
| P1 | 로컬 성공 JOB-ALIO 화면 수집이 GitHub에서 일시 실패 | 화면 서버 연결 시간 초과와 상세 페이지 다건 조회 | 운영 실행 35556143528의 `ConnectTimeout` | 별도 공식 ALIO 공개 JSON으로 교체, 제한 재시도·최근 정상 캐시·오류 분류 유지 |
| P2 | 출처가 갑자기 0건이어도 정상처럼 보일 수 있음 | 이전 건수·연속 실패 상태 미보관 | 기존 sources 테이블에 건수·실패 누적 필드 없음 | 이전/현재 건수, 연속 실패, `SOURCE_EMPTY_ANOMALY` 추가 |
| P2 | 화면에서 일부 실패와 전체 정상 구분이 약함 | 성공 수만 간단히 표시 | 기존 수집 상태 문구 | 실행 지표, 출처별 오류 유형·연속 실패·실제 감시 범위 표시 |

## 출처 전략

현재 활성 경로는 기업·기관 공식 채용페이지, 공식 그룹 채용시스템, 공식 공개 데이터, JOB-ALIO입니다. 공개 ATS·JSON-LD·사이트맵은 기업별 정책과 실제 구조가 확인되는 대로 같은 어댑터 계층에 추가합니다. 제3자 채용 플랫폼은 허용된 공개 API나 이용정책이 확인되기 전에는 자동 수집으로 위장하지 않습니다. 발견에 사용하더라도 `source_type`과 `data_confidence`로 공식 출처와 구분하도록 데이터 필드를 마련했습니다.

## 운영 원칙

GitHub Actions가 매일 00:00 UTC, 한국시간 09:00에 Python만 실행합니다. 일요일 01:00 UTC, 한국시간 10:00에는 넓은 범위로 실행합니다. OpenAI·ChatGPT·Codex·다른 LLM API와 유료 검색·스크래핑 서비스는 호출하지 않습니다. 한 출처가 실패해도 다른 출처 결과와 기존 공고를 보존합니다.
